# Vault DON Secrets Configuration
secretsNames:
  # AES-GCM encryption key for confidential HTTP responses
  san_marino_aes_gcm_encryption_key:
    - AES_ENCRYPTION_KEY

  # World ID API key (if needed for backend verification)
  world_id_api_key:
    - WORLD_ID_API_KEY
